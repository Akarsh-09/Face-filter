## Face Filter

# Description:
    An Computer Vision project using OpenCV which captures the camera of your device and applies filter onto the faces if detected in real-time(currently supports MacOS only).